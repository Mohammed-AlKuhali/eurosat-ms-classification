# Reproducibility: versions used to produce the reported results (Python 3.12,
# Apple Silicon M4 / PyTorch MPS). The data layer and Tier-1 classical baseline
# need only the top block; torch/torchgeo/timm are for the CNN arms.

# --- data layer + classical baseline + analysis ---
numpy==2.4.6
pandas==3.0.3
scikit-learn==1.9.0
tifffile==2026.6.1
PyYAML==6.0.3
matplotlib==3.11.0
tqdm==4.68.2
statsmodels==0.14.4          # McNemar (analysis)
scikit-image==0.24.0         # Otsu thresholding (segmentation extension)
pytest==9.1.0                # test suite (band order, split integrity, indices)

# --- CNN arms ---
torch==2.5.1
torchvision==0.20.1
timm==1.0.11                 # conv1 inflation via in_chans=N
torchgeo==0.6.2              # SSL4EO-S12 Sentinel-2 pretrained weights
grad-cam==1.5.4              # Grad-CAM explainability
